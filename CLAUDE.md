# プロジェクト方針

BOOTHの髪型商品を対象にした画像類似検索システムの実現可能性検証。

## クロールの約束事（厳守）

- booth.pm へのリクエストは 1〜2秒間隔、必ず直列。並列化・非同期の同時実行は禁止
- User-Agent に連絡先（メールアドレスまたはX ID）を明記する
- 商品ファイル本体（zip/unitypackage等）は絶対にダウンロードしない。
  取得してよいのは商品ページのHTML情報とサムネイル画像のみ
- robots.txt を尊重する
- 取得したHTMLと画像は必ず `cache/` 配下にローカル保存し、
  再実行時は必ずキャッシュを参照する。同じURLを二度取りに行かない
- エラー時は指数バックオフ。連続失敗したら止まって報告する

## 実行環境の約束事

- GPU（ROCm）は使わない。推論は ONNX Runtime の CPU で回す
- 依存関係は最小限に。環境構築で詰まったら、代替ライブラリを探す前に一度報告する
- Python は venv を切って使う

## 進め方

- 指示されたステップだけ実行して停止する。次のステップに勝手に進まない
- 各ステップの終わりに、結果のサンプルを表示して判断を仰ぐ
